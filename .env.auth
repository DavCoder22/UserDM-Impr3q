# Configuración de autenticación JWT
JWT_PRIVATE_KEY="$(cat config/keys/private_key.pem)"
JWT_PUBLIC_KEY="$(cat config/keys/public_key.pem)"
JWT_ALGORITHM=RS256
JWT_ISSUER=userdm-auth
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
